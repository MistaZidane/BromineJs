// the main widget class every other element will inherit from
 class Widget {
     constructor(type, atributes, style, event){
         this.name = type;
         this.atributes = atributes;
         this.style = style;
         this.events = event;
     }
 }